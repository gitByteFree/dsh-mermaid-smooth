/**
 * dsh-mermaid-smooth — mermaid engine entry.
 *
 * Bundled into lib/client.js by esbuild (the dynamic import in client.js
 * resolves to this private copy — zero CDN). Only the default mermaid API is
 * re-exported so the diagram-definitions tree ships once.
 */
import mermaid from "mermaid";
export default mermaid;
